# 测试：方块计划刻调度器-区块

本文件是区块数据（反）序列化数据集的专项约定；公共环境（SNBT 辅助函数、schema、编码与注释规则）见本目录 `README.md`。

两个实现都能读 V1 遗留格式与 V2 的两种子格式（打包、装箱），但同一份数据在两个实现下的结果并不总是相同，因此样例按"两个实现都适用"与"只对一个实现成立"分目录存放。

## 目录结构

```
data/world/schedule/
├── common/
│   ├── （反）序列化/         两个实现都适用的反序列化样例
│   └── 序列化/               两个实现都适用的序列化样例（当前为空）
├── packed/
│   ├── （反）序列化/         只对打包实现成立的反序列化样例
│   └── 序列化/               打包实现的序列化样例
└── boxed/
    ├── （反）序列化/         只对装箱实现成立的反序列化样例
    └── 序列化/               装箱实现的序列化样例
```

数据集说明只有两份：所有反序列化样例（含 `packed/`、`boxed/` 下的）记在 `common/（反）序列化/README.md`，所有序列化样例记在 `common/序列化/README.md`。`packed/`、`boxed/` 下不再设 README。

## 约定

- 每个测试类各扫两个目录，`common/` 加自己的专属目录：

  | 测试类 | 测试方法 | 扫描目录 |
  |---|---|---|
  | `区块打包计划刻测试` | `测试打包反序列化` | `common/（反）序列化/`、`packed/（反）序列化/` |
  | `区块打包计划刻测试` | `测试打包序列化` | `common/序列化/`、`packed/序列化/` |
  | `区块装箱计划刻测试` | `测试装箱反序列化` | `common/（反）序列化/`、`boxed/（反）序列化/` |
  | `区块装箱计划刻测试` | `测试装箱序列化` | `common/序列化/`、`boxed/序列化/` |

- `common/` 下的样例被两个实现各跑一遍，其答案必须对两个实现同时成立。答案依赖某一实现独有语义（坐标掩码、越界过滤、区块基址还原）的样例只能放专属目录；
- 样例成对出现，输入与答案主干同名、后缀不同：反序列化是 `.in.snbt` 配 `.ans.yaml`，序列化是 `.in.yaml` 配 `.ans.snbt`。配对由路径替换完成，缺一侧会在收集阶段失败；
- 文件名主干形如 `名字.X.Z`，`X`、`Z` 是区块坐标，可为负数。`common/` 与 `boxed/` 下的样例必须带区块坐标——装箱测试从文件名解析它来构造 `EmptyChunk`；`packed/` 下的样例不带，打包实现只处理区块内坐标，测试也不解析文件名坐标。

## 答案比较方式

反序列化答案（`.ans.yaml`）载入为 `计划刻数据`，由 `计划刻数据.假设相等` 与实际队列逐条配对比较：方块、坐标、触发刻、优先级四项全等即视为同一条，顺序无关，配上的从两边同时划去；答案多一条或输出多一条都报错。

序列化答案（`.ans.snbt`）先解析成 `NBTTagCompound`，再经 `NBTMatcher.toMatcher(expected,true)` 转成严格匹配器去匹配实际写出的复合标签。严格模式下：

- 复合标签的键集合必须精确相等，答案要把 `version`、`type`、`baseTime` 等键全部写出，不能只写关心的部分；
- 列表与数组按多重集合比较：元素数量必须一致、逐个配对划去，顺序无关。

多重集合语义在这里是必需的：打包实现的 `queue` 是把二叉堆的内部数组原样写出，装箱实现的 `entries` 按 `PriorityQueue` 的内部顺序写出，容器内部顺序都是实现细节，答案不应依赖具体排列。

## 新增样例

1. 判断答案是否对两个实现同时成立：成立放 `common/`，否则放 `packed/` 或 `boxed/`；
2. 按用途选配对后缀，两个文件主干保持一致；
3. 放进 `common/` 或 `boxed/` 时给主干补上 `.X.Z` 区块坐标；
4. `.snbt` 侧用 `blockOf`、`tick`、`packPosAndPri` 写打包值，`.yaml` 侧首行加 schema 注释；
5. 序列化答案要写全所有键，并确认不依赖容器内部顺序；
6. 在样例文件开头加一行注释，说明该样例记录的现状是什么——样例的说明只写在样例文件里，README 不维护样例清单。

*Claude Generated*
