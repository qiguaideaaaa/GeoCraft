---
# Claude Generated
name: 同刻齐发
ver: 1
default: 〇
ext:
  测试时段:
    - [0,3]
---
猹2 〇 猹2
〇 〇 〇
猹2 〇 〇

〇 〇 〇
〇 猹2 〇
〇 〇 〇

猹2 〇 猹2
〇 〇 〇
〇 〇 猹2

---
样例：同刻齐发（文档，解析器不读本节）

目标：同一区块内跨层、跨行列的七条计划同刻（t=2）批量消费。
布局：3×3×3 立方体，七只猹2 分布于三层（层1 三只、层2 中心一只、层3 三只），全部 时 2，测试时段 [0,3]。
故障与现场：任一条丢失或早消费（t=0/1 低四位 0/1 不同余）→ 对应位置猹幸存；触发刻被拆散到后续刻 → 迟到的消费不同余（或已在窗口外），猹幸存。
职责边界：七条 ≤ 单批容量（100），批处理的续弹与上限逻辑不在本样例覆盖范围（需百条级样例，违背"小"原则，明示放弃）；
七条全部缺省同级，同刻内部顺序不可观测——顺序由鲁迅样例的交互链覆盖；
七个位置在 x↔y 坐标互换后恰好还是原来那七个位置，该故障在本样例不会被发现（仍然通过），由跨区块样例的单行网格负责检出。
