---
# Claude Generated
name: 跨区块
ver: 1
default: 〇
ext:
  测试时段:
    - [0,4]
---
猹0 〇 〇 〇 〇 〇 〇 〇 〇 〇 〇 〇 〇 〇 〇 猹1 猹2 猹3

---
样例：跨区块（文档，解析器不读本节）

目标：计划分属两个相邻区块时各自独立入队、独立计时、独立消费。
布局：单层单行 18 列，列 16 是区块 0 的末列（x=15），列 17 起进入区块 1（x=16）。四只猹按 时 0–3 错峰，
出现时间与触发刻一一绑定，测试时段 [0,4]。
故障与现场：区块归属偏一（x=16 划入区块 0）→ 与列 1 的条目产生相同的去重键、被拒绝入队 → 猹2 幸存；
两区块共用数据或消费基址不刷新 → 错位消费落在空气或已清空的位置、被验证器跳过 → 本该被消费的猹幸存；
早/迟消费 → 四个触发刻低四位 0–3 互不相同，错开必幸存。
职责边界：正边界专用；负方向的符号类故障由负区块跨界样例负责。单行不对称布局同时兜住坐标互换类解码故障。
