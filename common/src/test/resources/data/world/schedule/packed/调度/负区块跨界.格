---
# Claude Generated
name: 负区块跨界
ver: 1
default: 〇
ext:
  测试时段:
    - [0,2]
---
猹0 猹1

---
样例：负区块跨界（文档，解析器不读本节）

目标：负坐标下的区块归属、区块内坐标掩码与消费基址全部正确。
布局：原点列取 0，基点 x=−1：猹0 落区块 −1（区块内 x 为 15），猹1 落区块 0（区块内 x 为 0），
时 0/1 错峰，测试时段 [0,2]。两格两条，是能触发负方向符号类故障的最小样例。
故障与现场：用 x/16 代替 x>>4（x=−1 误归区块 0）→ 消费落到 (15,0,0) 空气被跳过 → 猹0 幸存；
用 x%16 代替 x&0xF（区块内坐标 −1）→ 符号扩展污染存储的 delay成错误的远未来值 → 猹0 幸存；
消费基址清符号或两区块基址互换 → 落空跳过 → 幸存；早消费（猹1 于 t=0）→ 不同余幸存。
职责边界：只管负方向；正边界与双区块独立计时由跨区块样例负责。
