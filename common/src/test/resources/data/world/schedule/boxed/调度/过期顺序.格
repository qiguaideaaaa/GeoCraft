---
# Claude Generated
name: 过期顺序
ver: 1
default: 〇
ext:
  测试时段:
    - [0,1]
    - [8589934612,8589934613]
---
讯e猹3猹4

---
装箱专属：时钟跳跃拉开超过 2^31 的时间差后，同批过期条目必须按原触发刻顺序消费。
打包实现在这个量级会重设基准时间、把过期条目一并归 0，原顺序属实现细节不作保证；装箱存绝对触发刻，任意大的时间差都不该影响顺序，故本样例只放装箱侧。
讯（时5）在同一批消费中排在猹3位置的计划（时9）之前：讯先把猹3拨成猹4，随后该计划在 t=8589934612（低四位为4）消费时自清。
顺序颠倒则猹3先被消费（3≠4不自清）、再被讯拨成猹4留在原地——幸存者猹4在第1层第1行第2列即指认顺序断裂。
列3的猹4（创时与时均为8589934612，入队即当刻消费）不依赖顺序，探测的是"时间差为0的条目必须当刻消费"；对"全部立即消费"型退化实现的防护由时段 [0,1] 中的两条未来条目承担（t=0 提前引爆即留幸存者）。它不能在 t=0 入队——时间差装不进 int 会被压成严重过期标记值反而当场消费。
时间差验算：两个时段各恰好 1 刻；8589934612 = 2^33+20，低四位为 4。
